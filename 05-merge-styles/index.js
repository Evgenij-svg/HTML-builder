const fs = require("fs");
const path = require("path");
// Function to get current filenames
// in directory
fs.readdir("./05-merge-styles/styles", (err, files) => {
  files.forEach((file) => {
  
  });
});
